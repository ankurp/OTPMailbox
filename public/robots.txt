# robots.txt for OTP Inbox
# This service handles sensitive one-time-passcode data over its API.
# Only the homepage is public; everything else is off-limits to crawlers.

User-agent: *
Allow: /$
Disallow: /
